import { Box, Typography } from "@mui/material"

export const ErrorBoundary = () =>{
    return (
        <Box>
            <Typography variant="h2">Страница не найдена, попробуйте позже</Typography>
        </Box>
    )
}